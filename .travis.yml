language: go

services:
  - docker

before_install:
- docker build --no-cache -t travis:mytrav .
- docker run travis:mytrav /go/src/github.com//suryanaicker79/travisci-golang-example/travisci-golang-example.test
- docker run -d travis:mytrav
- docker run images

script:
  -docker run --help
  
  




